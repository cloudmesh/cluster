name "hadoop"
description "set Hadoop attributes"
default_attributes(
  "hadoop" => {
    "distribution" => "bigtop",
    "core_site" => {
      "fs.defaultFS" => "hdfs://{0}"
    },
    "yarn_site" => {
      "yarn.resourcemanager.hostname" => "{0}"
    }
  }
)
run_list(
  "recipe[hadoop]"
)  
